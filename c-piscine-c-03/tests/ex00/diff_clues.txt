  One number per line, so group by the pair of strings that produced it.
    * only the SIGN is wrong             -> the subject fixes what the sign
                                            means; which operand is subtracted
                                            from which
    * only pairs differing above 0x7f    -> whether char is signed here; the
                                            comparison is defined on unsigned
                                            values
    * equal strings diverge              -> the loop's exit condition when both
                                            reach the end together
    * prefixes diverge ("ab" vs "abc")   -> what is compared once one string has
                                            run out
    * the magnitude differs but the sign is right -> only the sign is specified
                                            here; if the fixture expects an
                                            exact value, it is the DIFFERENCE
