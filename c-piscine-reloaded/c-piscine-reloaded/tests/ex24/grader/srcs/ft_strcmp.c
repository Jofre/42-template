/*
 * The GRADER's ft_strcmp for ex24 -- "We'll only fetch your Makefile and test it
 * with our files". A PLACEHOLDER that compiles: ex24 grades the Makefile, not
 * this function, and the real body is the answer to another exercise.
 */
#include "ft.h"

int	ft_strcmp(char *s1, char *s2)
{
	(void)s1;
	(void)s2;
	return (0);
}
