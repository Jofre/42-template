#!/bin/sh
# ex08 — a file `clean` holding the command that deletes every editor backup
# file (Emacs's `*~` and `#*#`) anywhere in the current directory tree.
#
# Worth thinking about before you write it: those two patterns can match a
# DIRECTORY as well as a file, and deleting a directory is not the same
# operation as deleting a file — one of the two cases errors out, the other
# succeeds and takes something with it. Restrict what you match accordingly.
#
# (This comment survives into the shareable template, where the command below
# does not, so it says what to think about and not what to type. The other
# generators' comments are one line because their exercises need no warning;
# this one does.)
# TODO: write the commands that create this deliverable in the current dir.
:
