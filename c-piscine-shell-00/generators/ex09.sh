#!/bin/sh
# ex09 — a magic database ft_magic so that file(1) reports "42 file" for a file
# containing the string "42" at the 42nd byte (1-based) = magic offset 41 (0-based).
# TODO: write the commands that create this deliverable in the current dir.
:
