  A string and a separator set; the pieces are printed one per column.
    * leading or trailing separators -> those produce no piece
    * runs of separators      -> one boundary, not several empty pieces
    * an empty input          -> the array still exists and still ends somewhere
    * the last piece missing  -> the end of the string is a boundary too
    * the array is unterminated -> what marks the end of the array itself
