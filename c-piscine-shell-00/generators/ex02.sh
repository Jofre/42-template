#!/bin/sh
# ex02 — a tar archive of 7 items (dirs, files, a hardlink and a symlink) each
# with specific permissions and timestamps.
# TODO: write the commands that create this deliverable in the current dir.
:
