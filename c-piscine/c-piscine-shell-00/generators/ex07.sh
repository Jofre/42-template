#!/bin/sh
# ex07 — produce file b by applying the patch sw.diff to file a. Both a and
# sw.diff come from resources.tar.gz (placed in the current directory for you).
# TODO: write the commands that create this deliverable in the current dir.
:
