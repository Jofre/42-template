#!/bin/sh
# ex08 — add_chelou.sh: decode FT_NBR1/FT_NBR2 from two odd symbol alphabets into
# base-5 digits, add them, and re-encode the base-13 result into a third alphabet.
# TODO: write the commands that create this deliverable in the current dir.
:
