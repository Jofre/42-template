#!/bin/sh
# ex04 — a file midLS holding the command that lists the current directory as a
# comma-separated list, marking directories with a trailing slash, newest first.
# TODO: write the commands that create this deliverable in the current dir.
:
