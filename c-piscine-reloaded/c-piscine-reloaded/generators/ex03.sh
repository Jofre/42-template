#!/bin/sh
# ex03 — find_sh.sh -- a command line that prints the name of every .sh file here and
# in sub-directories, without the .sh.
# TODO: write the commands that create this deliverable in the current dir.
:
