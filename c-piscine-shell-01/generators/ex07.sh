#!/bin/sh
# ex07 — r_dwssap.sh: from /etc/passwd, take every 2nd login, reverse each, sort
# descending, keep lines FT_LINE1..FT_LINE2, join with ", " and end with a period.
# TODO: write the commands that create this deliverable in the current dir.
:
