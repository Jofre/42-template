#!/bin/sh
# ex09 — a magic file ft_magic that detects files of the type "42 file": files
# built with the string "42" at the 42nd byte.
# TODO: write the commands that create this deliverable in the current dir.
:
