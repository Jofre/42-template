#!/bin/sh
# ex00 — exo.tar -- a tar archive of test0..test6: directories, files, a hard link
# and a symbolic link, each with the permissions, sizes and dates the subject lists.
# TODO: write the commands that create this deliverable in the current dir.
:
