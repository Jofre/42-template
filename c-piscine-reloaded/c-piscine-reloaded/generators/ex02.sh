#!/bin/sh
# ex02 — clean -- one command line that shows and deletes every file, here and in
# sub-directories, whose name ends in ~ or starts and ends with #.
# TODO: write the commands that create this deliverable in the current dir.
:
