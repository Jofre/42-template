/*
 * The GRADER's header for ex24. A PLACEHOLDER, like the sources beside it:
 * every one of them includes it, so a Makefile that does not hand the compiler
 * the includes directory fails to build -- which is what the subject's "The
 * Makefile will get its header files from the includes directory" asks for.
 * Its real contents, the five prototypes, are C 08 ex00's whole answer, so they
 * are not written here: nothing in these placeholders needs them.
 */
#ifndef FT_H
# define FT_H

#endif
